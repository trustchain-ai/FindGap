# FindGap 1.0.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build FindGap 1.0.0 as a handoff-time gap checker with owner self-check and receiver handoff-check modes, a thin TypeScript CLI, aligned skill/docs/repo messaging, and soft-copyright-friendly supporting materials.

**Architecture:** Keep the existing skill as the source-of-truth behavior contract, then build a small TypeScript engine and CLI that codify the same handoff-gap-checking pipeline: parse artifact → detect mode → find rule hits → classify severity and evidence → format a fixed handoff review output. Treat docs, README, verification artifacts, and sibling repo messaging as synchronization work that follows the implemented behavior.

**Tech Stack:** Markdown skill contract, TypeScript, Node.js 20+, Vitest, tsx, npm

---

## File Map

### Create
- `package.json` — npm package metadata, scripts, dev dependencies, CLI bin entry
- `tsconfig.json` — TypeScript compiler configuration for CLI + engine + eval modules
- `cli/index.ts` — CLI entrypoint and command router
- `cli/config.ts` — CLI configuration defaults and path helpers
- `cli/commands/check.ts` — handoff check command for a single artifact
- `cli/commands/eval.ts` — fixture-suite evaluation command
- `cli/commands/report.ts` — summary report command over eval output
- `engine/contracts.ts` — immutable shared types and fixed output contract
- `engine/parser.ts` — artifact parsing and line-anchor normalization
- `engine/audience.ts` — self-check vs receiver-check perspective logic
- `engine/detector.ts` — 11-rule detection orchestration with handoff-first priority order
- `engine/severity.ts` — severity classification logic
- `engine/evidence.ts` — evidence-type classification (`public_evidence`, `contextual_inference`, `insufficient_information`)
- `engine/fallback.ts` — uncertainty and insufficiency handling
- `engine/advisor.ts` — optional fill-in direction generation, secondary to detection
- `engine/formatter.ts` — fixed output shape and flat-list formatter
- `engine/index.ts` — engine composition entrypoint
- `eval/runner.ts` — run fixture suites and collect results
- `eval/reporter.ts` — aggregate fixture results into regression summaries
- `tests/fixtures/rule-coverage/cases.ts` — fixture cases covering 11 rules with at least 33 total cases
- `docs/user-manual.md` — software-style user manual for soft copyright package
- `docs/architecture.md` — architecture and module interaction doc
- `docs/originality.md` — originality, dependency, and rights statement

### Modify
- `skill/FindGap.skill.md` — lock 1.0.0 wording, handoff-time scenario, self-check / receiver-check modes, fixed output contract
- `manifest.json` — version `1.0.0`, description, trigger surface alignment
- `README.md` — reposition around handoff-time gap checking, ROI, result-oriented value, usage modes
- `tests/verify-skill.md` — verification criteria for new wording, new output fields, and no-scope-drift rules
- `CHANGELOG.md` — add 1.0.0 release section
- `ROADMAP.md` — align milestones to 1.0.0 implementation and post-1.0 direction
- `CONTRIBUTING.md` — update contribution guidance for TypeScript CLI/engine/tests/docs
- `FindAction/README.md` — demote to experimental / roadmap status and point to FindGap
- `FindMiss/README.md` — demote to experimental / roadmap status and point to FindGap

### Test
- `tests/verify-skill.md`
- `tests/fixtures/rule-coverage/cases.ts`
- Additional task-local test files shown below under each task

---

### Task 1: Lock the 1.0.0 contract and repo wording

**Files:**
- Modify: `skill/FindGap.skill.md`
- Modify: `manifest.json`
- Modify: `README.md`
- Test: `tests/verify-skill.md`

- [ ] **Step 1: Write the failing contract test**

```ts
// tests/contract/findgap-contract.test.ts
import { describe, expect, it } from 'vitest'
import { readFileSync } from 'node:fs'

const skill = readFileSync('skill/FindGap.skill.md', 'utf8')
const manifest = readFileSync('manifest.json', 'utf8')
const readme = readFileSync('README.md', 'utf8')

describe('FindGap 1.0.0 contract', () => {
  it('locks handoff-time positioning and dual modes', () => {
    expect(skill).toContain('1.0.0')
    expect(skill).toContain('Self-check')
    expect(skill).toContain('Receiver-check')
    expect(skill).toContain('交接')
    expect(skill).not.toContain('跨模型验证')

    expect(manifest).toContain('1.0.0')
    expect(readme).toContain('handoff')
    expect(readme).toContain('ROI')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/contract/findgap-contract.test.ts`
Expected: FAIL with missing `package.json` or missing `1.0.0` / handoff wording

- [ ] **Step 3: Write minimal package bootstrap so tests can run**

```json
// package.json
{
  "name": "findgap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "findgap": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
```

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "rootDir": ".",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": false
  },
  "include": [
    "cli/**/*.ts",
    "engine/**/*.ts",
    "eval/**/*.ts",
    "tests/**/*.ts"
  ]
}
```

- [ ] **Step 4: Install dev dependencies**

Run: `npm install`
Expected: creates `package-lock.json` and installs 4 dev dependencies

- [ ] **Step 5: Update the skill, manifest, and README wording**

```md
<!-- skill/FindGap.skill.md snippet -->
# FindGap · 1.0.0 · 交接点 Gap 校对器

> **一句话**：当产出物所有者认为内容已达到自己认知内的最优水平、准备交给下一个人或 agent 时，FindGap 识别并校对那些仍未被看见、但会在下游引发高成本返工的关键 gap。

## 双模式
- **Self-check**：所有者自检
- **Receiver-check**：接头人他检
```

```json
// manifest.json snippet
{
  "version": "1.0.0",
  "displayName": "FindGap",
  "description": "交接点 Gap 校对器：在 handoff 前识别 owner 和 receiver 都可能忽略的高成本 gap，减少返工并最大化 ROI。"
}
```

```md
<!-- README.md opening snippet -->
# FindGap

> **当你准备把产物交给下一个人或 agent 时，先校对 gap。**
>
> FindGap 不是每轮对话前的提示词优化器，而是交接前最后一道认知校对：在 owner 自认内容已经足够好的时刻，识别那些仍会在下游引发返工的关键 gap。

## 两种使用方式
- **Self-check**：产出物所有者在 handoff 前自检
- **Receiver-check**：接头人在接手前做他检

## 结果导向价值
- 减少返工
- 最大化 ROI
- 促进自我成长
- 提升价值输出
```

- [ ] **Step 6: Run contract test to verify it passes**

Run: `npm test -- tests/contract/findgap-contract.test.ts`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json tsconfig.json skill/FindGap.skill.md manifest.json README.md tests/contract/findgap-contract.test.ts
git commit -m "feat: lock findgap 1.0.0 contract"
```

### Task 2: Define shared engine contracts and handoff modes

**Files:**
- Create: `engine/contracts.ts`
- Test: `tests/engine/contracts.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/contracts.test.ts
import { describe, expect, it } from 'vitest'
import { CATEGORY_ORDER, MODE_ORDER } from '../../engine/contracts'

describe('engine contracts', () => {
  it('defines the approved handoff modes and output priority order', () => {
    expect(MODE_ORDER).toEqual(['self-check', 'receiver-check'])
    expect(CATEGORY_ORDER).toEqual([
      'completion-acceptance-gap',
      'boundary-failure-gap',
      'terminology-definition-gap',
      'method-constraint-dependency-gap'
    ])
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/contracts.test.ts`
Expected: FAIL with `Cannot find module '../../engine/contracts'`

- [ ] **Step 3: Write minimal implementation**

```ts
// engine/contracts.ts
export const MODE_ORDER = ['self-check', 'receiver-check'] as const
export type FindGapMode = (typeof MODE_ORDER)[number]

export const CATEGORY_ORDER = [
  'completion-acceptance-gap',
  'boundary-failure-gap',
  'terminology-definition-gap',
  'method-constraint-dependency-gap'
] as const
export type PublicCategory = (typeof CATEGORY_ORDER)[number]

export const RULE_PRIORITY = [
  'R-DOD',
  'V-STAKE',
  'V-NAME',
  'I-SSOT',
  'G-NOGO',
  'I-ADR',
  'S-PERF',
  'S-QUANT',
  'S-NFR',
  'G-WHY',
  'V-LAYER'
] as const
export type RuleCode = (typeof RULE_PRIORITY)[number]

export type Severity = 'blocking' | 'warning' | 'defer'
export type EvidenceType = 'public-evidence' | 'contextual-inference' | 'insufficient-information'
export type InputKind = 'one-line-request' | 'technical-plan' | 'prd-fragment' | 'decision-request' | 'unknown'

export interface SourceAnchor {
  lineStart: number
  lineEnd: number
  quote: string
}

export interface ArtifactInput {
  mode: FindGapMode
  inputKind: InputKind
  rawText: string
  lines: string[]
}

export interface GapFinding {
  ruleCode: RuleCode
  publicCategory: PublicCategory
  severity: Severity
  anchor: SourceAnchor
  gap: string
  downstreamRisk: string
  evidenceType: EvidenceType
  fillInDirection?: string
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/contracts.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/contracts.ts tests/engine/contracts.test.ts
git commit -m "feat: define findgap engine contracts"
```

### Task 3: Parse artifacts and detect owner vs receiver perspective

**Files:**
- Create: `engine/parser.ts`
- Create: `engine/audience.ts`
- Test: `tests/engine/parser-audience.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/parser-audience.test.ts
import { describe, expect, it } from 'vitest'
import { parseArtifact } from '../../engine/parser'
import { describePerspective } from '../../engine/audience'

describe('parser and audience', () => {
  it('normalizes text and classifies a technical handoff artifact', () => {
    const input = parseArtifact({
      mode: 'self-check',
      rawText: '需求：支持 BNPL\n验收：待补充\n范围：先做印尼'
    })

    expect(input.inputKind).toBe('technical-plan')
    expect(input.lines).toHaveLength(3)

    expect(describePerspective(input.mode)).toEqual({
      role: 'owner',
      question: 'If I hand this off now, what will the next person misunderstand or need to ask back about?'
    })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/parser-audience.test.ts`
Expected: FAIL with missing parser/audience modules

- [ ] **Step 3: Write minimal implementation**

```ts
// engine/parser.ts
import type { ArtifactInput, FindGapMode, InputKind } from './contracts'

function detectInputKind(rawText: string): InputKind {
  if (/PRD|需求|验收|范围|方案/u.test(rawText)) return 'technical-plan'
  if (/决策|trade-off|why/u.test(rawText)) return 'decision-request'
  if (rawText.split('\n').length <= 2) return 'one-line-request'
  return 'unknown'
}

export function parseArtifact(input: { mode: FindGapMode; rawText: string }): ArtifactInput {
  return {
    mode: input.mode,
    inputKind: detectInputKind(input.rawText),
    rawText: input.rawText,
    lines: input.rawText.split('\n')
  }
}
```

```ts
// engine/audience.ts
import type { FindGapMode } from './contracts'

export function describePerspective(mode: FindGapMode) {
  if (mode === 'self-check') {
    return {
      role: 'owner',
      question: 'If I hand this off now, what will the next person misunderstand or need to ask back about?'
    }
  }

  return {
    role: 'receiver',
    question: 'If I receive this cold, what blocks me from executing correctly and confidently?'
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/parser-audience.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/parser.ts engine/audience.ts tests/engine/parser-audience.test.ts
git commit -m "feat: add artifact parsing and perspective handling"
```

### Task 4: Implement the first handoff-critical rules end to end

**Files:**
- Create: `engine/detector.ts`
- Create: `engine/severity.ts`
- Test: `tests/engine/handoff-priority-rules.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/handoff-priority-rules.test.ts
import { describe, expect, it } from 'vitest'
import { parseArtifact } from '../../engine/parser'
import { detectRuleHits } from '../../engine/detector'
import { applySeverity } from '../../engine/severity'

describe('handoff priority rules', () => {
  it('detects missing acceptance, failure-path, and no-go boundaries', () => {
    const artifact = parseArtifact({
      mode: 'self-check',
      rawText: [
        '目标：支持先买后付结账能力',
        '范围：先做印尼',
        '上线尽快'
      ].join('\n')
    })

    const hits = applySeverity(detectRuleHits(artifact))

    expect(hits.map(hit => hit.ruleCode)).toEqual(
      expect.arrayContaining(['R-DOD', 'V-STAKE', 'G-NOGO'])
    )
    expect(hits.find(hit => hit.ruleCode === 'R-DOD')?.severity).toBe('blocking')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/handoff-priority-rules.test.ts`
Expected: FAIL with missing detector/severity modules

- [ ] **Step 3: Write minimal implementation**

```ts
// engine/detector.ts
import type { ArtifactInput, GapFinding, RuleCode } from './contracts'

function finding(ruleCode: RuleCode, line: number, quote: string, gap: string, downstreamRisk: string): GapFinding {
  return {
    ruleCode,
    publicCategory:
      ruleCode === 'R-DOD'
        ? 'completion-acceptance-gap'
        : 'boundary-failure-gap',
    severity: 'warning',
    anchor: { lineStart: line, lineEnd: line, quote },
    gap,
    downstreamRisk,
    evidenceType: 'insufficient-information'
  }
}

export function detectRuleHits(input: ArtifactInput): GapFinding[] {
  const findings: GapFinding[] = []
  const text = input.rawText
  const lines = input.lines

  if (!/验收|完成|done|通过标准/u.test(text)) {
    findings.push(
      finding(
        'R-DOD',
        1,
        lines[0] ?? '',
        '缺少可观察的完成定义或验收标准。',
        '下游会在不同完成标准下继续推进，导致返工。'
      )
    )
  }

  if (!/失败|异常|回滚|降级|受影响方/u.test(text)) {
    findings.push(
      finding(
        'V-STAKE',
        1,
        lines[0] ?? '',
        '缺少失败路径、回滚、降级或受影响方说明。',
        '下游在异常场景下会默认补脑，联调和验收时容易返工。'
      )
    )
  }

  if (!/不做|排除|优先级|先不/u.test(text)) {
    findings.push(
      finding(
        'G-NOGO',
        1,
        lines[0] ?? '',
        '缺少明确的不做项或边界约束。',
        '下游会扩大范围或误判优先级，造成额外返工。'
      )
    )
  }

  return findings
}
```

```ts
// engine/severity.ts
import type { GapFinding } from './contracts'

export function applySeverity(findings: GapFinding[]): GapFinding[] {
  return findings.map(finding => ({
    ...finding,
    severity: finding.ruleCode === 'R-DOD' ? 'blocking' : 'warning'
  }))
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/handoff-priority-rules.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/detector.ts engine/severity.ts tests/engine/handoff-priority-rules.test.ts
git commit -m "feat: detect primary handoff blockers"
```

### Task 5: Implement terminology and dependency/constraint rules

**Files:**
- Modify: `engine/detector.ts`
- Test: `tests/engine/definition-constraint-rules.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/definition-constraint-rules.test.ts
import { describe, expect, it } from 'vitest'
import { parseArtifact } from '../../engine/parser'
import { detectRuleHits } from '../../engine/detector'

describe('definition and constraint rules', () => {
  it('detects terminology, ssot, decision, and dependency gaps', () => {
    const artifact = parseArtifact({
      mode: 'receiver-check',
      rawText: [
        '支持 BNPL 结账能力',
        '接口以 portal 字段为准',
        '性能要好，体验流畅',
        '先接 Doku，再接其他渠道'
      ].join('\n')
    })

    const hits = detectRuleHits(artifact)

    expect(hits.map(hit => hit.ruleCode)).toEqual(
      expect.arrayContaining(['V-NAME', 'I-SSOT', 'I-ADR', 'S-PERF'])
    )
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/definition-constraint-rules.test.ts`
Expected: FAIL because the new rule hits are not implemented

- [ ] **Step 3: Extend detector minimally**

```ts
// engine/detector.ts additions inside detectRuleHits
  if (/BNPL/u.test(text) && !/定义|指的是|期数|免息/u.test(text)) {
    findings.push(
      finding(
        'V-NAME',
        1,
        lines.find(line => /BNPL/u.test(line)) ?? '',
        '关键术语未定义，存在同名异义风险。',
        '下游可能按不同产品形态理解同一术语，导致接口或流程返工。'
      )
    )
  }

  if (/为准/u.test(text) && !/文档|链接|来源|SSOT/u.test(text)) {
    findings.push(
      finding(
        'I-SSOT',
        2,
        lines.find(line => /为准/u.test(line)) ?? '',
        '提到了权威口径，但没有给出单一事实源。',
        '下游会在多个来源之间自行猜测，造成定义漂移。'
      )
    )
  }

  if (/先接|选用|采用/u.test(text) && !/原因|为什么|trade-off/u.test(text)) {
    findings.push(
      finding(
        'I-ADR',
        4,
        lines.find(line => /先接|选用|采用/u.test(line)) ?? '',
        '关键决策缺少最小理由。',
        '下游无法判断何时坚持该决策、何时应该调整。'
      )
    )
  }

  if (/性能要好|体验流畅/u.test(text)) {
    findings.push(
      finding(
        'S-PERF',
        3,
        lines.find(line => /性能要好|体验流畅/u.test(line)) ?? '',
        '性能或体验要求缺少可量化约束。',
        '下游无法据此设计或验收，会在联调时返工。'
      )
    )
  }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/definition-constraint-rules.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/detector.ts tests/engine/definition-constraint-rules.test.ts
git commit -m "feat: detect definition and constraint gaps"
```

### Task 6: Add evidence, uncertainty, and fill-in-direction modules

**Files:**
- Create: `engine/evidence.ts`
- Create: `engine/fallback.ts`
- Create: `engine/advisor.ts`
- Test: `tests/engine/evidence-fallback-advisor.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/evidence-fallback-advisor.test.ts
import { describe, expect, it } from 'vitest'
import type { GapFinding } from '../../engine/contracts'
import { classifyEvidence } from '../../engine/evidence'
import { applyFallback } from '../../engine/fallback'
import { addFillInDirections } from '../../engine/advisor'

const finding: GapFinding = {
  ruleCode: 'R-DOD',
  publicCategory: 'completion-acceptance-gap',
  severity: 'blocking',
  anchor: { lineStart: 1, lineEnd: 1, quote: '支持 BNPL 结账能力' },
  gap: '缺少验收标准。',
  downstreamRisk: '下游无法判断是否完成。',
  evidenceType: 'insufficient-information'
}

describe('evidence, fallback, and advisor', () => {
  it('assigns evidence type, marks uncertainty, and adds fill-in direction', () => {
    const withEvidence = classifyEvidence([finding])
    const withFallback = applyFallback(withEvidence)
    const withAdvice = addFillInDirections(withFallback)

    expect(withAdvice[0].evidenceType).toBe('insufficient-information')
    expect(withAdvice[0].fillInDirection).toContain('补充')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/evidence-fallback-advisor.test.ts`
Expected: FAIL with missing modules

- [ ] **Step 3: Write minimal implementation**

```ts
// engine/evidence.ts
import type { GapFinding } from './contracts'

export function classifyEvidence(findings: GapFinding[]): GapFinding[] {
  return findings.map(finding => {
    if (finding.ruleCode === 'I-SSOT') {
      return { ...finding, evidenceType: 'contextual-inference' }
    }
    return finding
  })
}
```

```ts
// engine/fallback.ts
import type { GapFinding } from './contracts'

export function applyFallback(findings: GapFinding[]): GapFinding[] {
  return findings.map(finding => {
    if (finding.evidenceType === 'insufficient-information') {
      return {
        ...finding,
        downstreamRisk: `${finding.downstreamRisk} 当前信息不足，需要显式澄清。`
      }
    }

    return finding
  })
}
```

```ts
// engine/advisor.ts
import type { GapFinding } from './contracts'

export function addFillInDirections(findings: GapFinding[]): GapFinding[] {
  return findings.map(finding => ({
    ...finding,
    fillInDirection: `补充与 ${finding.ruleCode} 对应的最小必要信息，供下游正确接手。`
  }))
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/evidence-fallback-advisor.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/evidence.ts engine/fallback.ts engine/advisor.ts tests/engine/evidence-fallback-advisor.test.ts
git commit -m "feat: add evidence and fallback handling"
```

### Task 7: Format the handoff review output

**Files:**
- Create: `engine/formatter.ts`
- Create: `engine/index.ts`
- Test: `tests/engine/formatter.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/engine/formatter.test.ts
import { describe, expect, it } from 'vitest'
import type { GapFinding } from '../../engine/contracts'
import { formatFindings } from '../../engine/formatter'

const findings: GapFinding[] = [
  {
    ruleCode: 'R-DOD',
    publicCategory: 'completion-acceptance-gap',
    severity: 'blocking',
    anchor: { lineStart: 1, lineEnd: 1, quote: '支持 BNPL 结账能力' },
    gap: '缺少验收标准。',
    downstreamRisk: '下游无法判断是否完成。',
    evidenceType: 'insufficient-information',
    fillInDirection: '补充完成定义。'
  }
]

describe('formatter', () => {
  it('renders the approved flat output contract', () => {
    const output = formatFindings(findings)

    expect(output).toContain('FindGap · 发现 1 处 gap 可能导致返工')
    expect(output).toContain('原文："支持 BNPL 结账能力"')
    expect(output).toContain('下游风险：下游无法判断是否完成。')
    expect(output).toContain('依据类型：insufficient-information')
    expect(output).toContain('补齐方向：补充完成定义。')
    expect(output).not.toContain('PASS')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/engine/formatter.test.ts`
Expected: FAIL with missing formatter module

- [ ] **Step 3: Write minimal implementation**

```ts
// engine/formatter.ts
import type { GapFinding } from './contracts'

const ICONS = {
  blocking: '🔴',
  warning: '🟠',
  defer: '🟡'
} as const

export function formatFindings(findings: GapFinding[]): string {
  const body = findings
    .map(finding => {
      return [
        `${ICONS[finding.severity]} ${finding.publicCategory}`,
        `原文："${finding.anchor.quote}"`,
        `缺口：${finding.gap}`,
        `下游风险：${finding.downstreamRisk}`,
        `依据类型：${finding.evidenceType}`,
        finding.fillInDirection ? `补齐方向：${finding.fillInDirection}` : ''
      ]
        .filter(Boolean)
        .join('\n')
    })
    .join('\n\n---\n\n')

  return [`FindGap · 发现 ${findings.length} 处 gap 可能导致返工`, '', '---', '', body].join('\n')
}
```

```ts
// engine/index.ts
import { parseArtifact } from './parser'
import { detectRuleHits } from './detector'
import { applySeverity } from './severity'
import { classifyEvidence } from './evidence'
import { applyFallback } from './fallback'
import { addFillInDirections } from './advisor'
import { formatFindings } from './formatter'
import type { FindGapMode } from './contracts'

export function runFindGap(mode: FindGapMode, rawText: string) {
  const artifact = parseArtifact({ mode, rawText })
  const findings = addFillInDirections(
    applyFallback(classifyEvidence(applySeverity(detectRuleHits(artifact))))
  )

  return {
    artifact,
    findings,
    output: formatFindings(findings)
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/engine/formatter.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add engine/formatter.ts engine/index.ts tests/engine/formatter.test.ts
git commit -m "feat: format handoff review output"
```

### Task 8: Add the CLI check command

**Files:**
- Create: `cli/index.ts`
- Create: `cli/config.ts`
- Create: `cli/commands/check.ts`
- Test: `tests/cli/check-command.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/cli/check-command.test.ts
import { describe, expect, it } from 'vitest'
import { writeFileSync, mkdirSync, rmSync } from 'node:fs'
import { join } from 'node:path'
import { runCheckCommand } from '../../cli/commands/check'

describe('check command', () => {
  it('runs self-check on a markdown artifact', () => {
    const dir = '.tmp-tests'
    mkdirSync(dir, { recursive: true })
    const file = join(dir, 'artifact.md')
    writeFileSync(file, '目标：支持 BNPL\n范围：先做印尼\n上线尽快')

    const output = runCheckCommand({ filePath: file, mode: 'self-check' })

    expect(output).toContain('FindGap · 发现')
    expect(output).toContain('R-DOD')

    rmSync(dir, { recursive: true, force: true })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/cli/check-command.test.ts`
Expected: FAIL with missing check command

- [ ] **Step 3: Write minimal implementation**

```ts
// cli/config.ts
export const DEFAULT_MODE = 'self-check' as const
```

```ts
// cli/commands/check.ts
import { readFileSync } from 'node:fs'
import { runFindGap } from '../../engine/index'
import type { FindGapMode } from '../../engine/contracts'

export function runCheckCommand(input: { filePath: string; mode: FindGapMode }) {
  const rawText = readFileSync(input.filePath, 'utf8')
  return runFindGap(input.mode, rawText).output
}
```

```ts
// cli/index.ts
import { DEFAULT_MODE } from './config'
import { runCheckCommand } from './commands/check'

const [, , command, filePath, maybeMode] = process.argv

if (command === 'check' && filePath) {
  const output = runCheckCommand({
    filePath,
    mode: maybeMode === 'receiver-check' ? 'receiver-check' : DEFAULT_MODE
  })
  process.stdout.write(`${output}\n`)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/cli/check-command.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/config.ts cli/commands/check.ts cli/index.ts tests/cli/check-command.test.ts
git commit -m "feat: add handoff check cli command"
```

### Task 9: Add eval and report commands with a fixture suite

**Files:**
- Create: `eval/runner.ts`
- Create: `eval/reporter.ts`
- Create: `cli/commands/eval.ts`
- Create: `cli/commands/report.ts`
- Create: `tests/fixtures/rule-coverage/cases.ts`
- Test: `tests/eval/runner.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/eval/runner.test.ts
import { describe, expect, it } from 'vitest'
import { runFixtureSuite } from '../../eval/runner'
import { RULE_COVERAGE_CASES } from '../fixtures/rule-coverage/cases'

describe('fixture suite', () => {
  it('executes all rule coverage cases', () => {
    const results = runFixtureSuite(RULE_COVERAGE_CASES)

    expect(results).toHaveLength(33)
    expect(results.every(result => result.pass)).toBe(true)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/eval/runner.test.ts`
Expected: FAIL with missing eval modules or fixture cases

- [ ] **Step 3: Write minimal implementation and fixtures**

```ts
// tests/fixtures/rule-coverage/cases.ts
import type { FindGapMode, RuleCode } from '../../../engine/contracts'

export interface RuleCoverageCase {
  name: string
  mode: FindGapMode
  text: string
  expectedRule: RuleCode
}

const makeCases = (rule: RuleCode, cases: string[]): RuleCoverageCase[] =>
  cases.map((text, index) => ({
    name: `${rule}-${index + 1}`,
    mode: index % 2 === 0 ? 'self-check' : 'receiver-check',
    text,
    expectedRule: rule
  }))

export const RULE_COVERAGE_CASES: RuleCoverageCase[] = [
  ...makeCases('R-DOD', [
    '目标：支持 BNPL 结账能力\n范围：先做印尼\n上线尽快',
    '方案：改造退款流程\n依赖：Portal 2.0\n排期：本周内',
    '需求：支持关单机制\n涉及角色：收银员'
  ]),
  ...makeCases('V-STAKE', [
    '目标：统一支付结果上报\n验收：上线成功',
    '方案：合并渠道回调\n范围：先支持 Doku',
    '需求：修复串口打印超时\n完成标准：不再白屏'
  ]),
  ...makeCases('V-NAME', [
    '支持 BNPL 结账能力\n验收：成功展示',
    '支持 settlement 查询\n范围：印尼商户',
    '对接 capture 流程\n完成标准：接口可调用'
  ]),
  ...makeCases('I-SSOT', [
    '接口以 portal 字段为准\n验收：字段对齐',
    '账单展示以 MIS 为准\n上线尽快',
    '订单状态以渠道回调为准\n处理人：后端'
  ]),
  ...makeCases('G-NOGO', [
    '目标：同时提高成功率、降低成本、缩短开发周期',
    '方案：先做印尼渠道接入\n上线尽快',
    '需求：支持全部钱包方式\n范围：后续再说'
  ]),
  ...makeCases('I-ADR', [
    '先接 Doku，再接其他渠道',
    '采用 portal 字段模型作为标准',
    '先上静态码，再考虑动态码'
  ]),
  ...makeCases('S-PERF', [
    '性能要好，体验流畅',
    '接口响应要快',
    '页面打开要稳定且迅速'
  ]),
  ...makeCases('S-QUANT', [
    '支持所有支付方式',
    '任何异常都要处理',
    '全部状态都要同步'
  ]),
  ...makeCases('S-NFR', [
    '系统必须稳定可靠',
    '要保证安全合规',
    '流程需要健壮'
  ]),
  ...makeCases('G-WHY', [
    '改造结账流程\n交付时间：本周',
    '支持新渠道接入\n验收：接口联通',
    '新增 Portal 字段\n责任人：前端'
  ]),
  ...makeCases('V-LAYER', [
    '业务上统一账单语义，实现上沿用旧字段，接口按新名返回',
    '产品定义退款成功，技术上只记录 processing，运营按成功口径展示',
    '结账体验保持一致，接口分层暂不统一，文档按新结构描述'
  ])
]
```

```ts
// eval/runner.ts
import { runFindGap } from '../engine/index'
import type { RuleCoverageCase } from '../tests/fixtures/rule-coverage/cases'

export function runFixtureSuite(cases: RuleCoverageCase[]) {
  return cases.map(testCase => {
    const result = runFindGap(testCase.mode, testCase.text)
    return {
      name: testCase.name,
      pass: result.findings.some(finding => finding.ruleCode === testCase.expectedRule)
    }
  })
}
```

```ts
// eval/reporter.ts
export function summarizeFixtureResults(results: Array<{ name: string; pass: boolean }>) {
  const passed = results.filter(result => result.pass).length
  return {
    total: results.length,
    passed,
    failed: results.length - passed
  }
}
```

```ts
// cli/commands/eval.ts
import { RULE_COVERAGE_CASES } from '../../tests/fixtures/rule-coverage/cases'
import { runFixtureSuite } from '../../eval/runner'

export function runEvalCommand() {
  return runFixtureSuite(RULE_COVERAGE_CASES)
}
```

```ts
// cli/commands/report.ts
import { RULE_COVERAGE_CASES } from '../../tests/fixtures/rule-coverage/cases'
import { runFixtureSuite } from '../../eval/runner'
import { summarizeFixtureResults } from '../../eval/reporter'

export function runReportCommand() {
  return summarizeFixtureResults(runFixtureSuite(RULE_COVERAGE_CASES))
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/eval/runner.test.ts`
Expected: PASS with 33 passing fixture cases

- [ ] **Step 5: Commit**

```bash
git add eval/runner.ts eval/reporter.ts cli/commands/eval.ts cli/commands/report.ts tests/fixtures/rule-coverage/cases.ts tests/eval/runner.test.ts
git commit -m "feat: add fixture-based regression runner"
```

### Task 10: Update verification docs and changelog

**Files:**
- Modify: `tests/verify-skill.md`
- Modify: `CHANGELOG.md`
- Modify: `ROADMAP.md`
- Modify: `CONTRIBUTING.md`

- [ ] **Step 1: Write the failing doc-sync test**

```ts
// tests/docs/doc-sync.test.ts
import { describe, expect, it } from 'vitest'
import { readFileSync } from 'node:fs'

const verifySkill = readFileSync('tests/verify-skill.md', 'utf8')
const changelog = readFileSync('CHANGELOG.md', 'utf8')
const roadmap = readFileSync('ROADMAP.md', 'utf8')
const contributing = readFileSync('CONTRIBUTING.md', 'utf8')

describe('doc sync', () => {
  it('documents the 1.0.0 handoff checker behavior', () => {
    expect(verifySkill).toContain('Self-check')
    expect(verifySkill).toContain('Receiver-check')
    expect(changelog).toContain('1.0.0')
    expect(roadmap).toContain('交接点')
    expect(contributing).toContain('TypeScript')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/docs/doc-sync.test.ts`
Expected: FAIL on missing 1.0.0 handoff wording

- [ ] **Step 3: Update docs minimally**

```md
<!-- tests/verify-skill.md additions -->
- [ ] 支持 Self-check（所有者自检）
- [ ] 支持 Receiver-check（接头人他检）
- [ ] 输出必须包含：下游风险、依据类型、补齐方向
- [ ] 输出不得包含：PASS / FAIL / 打分 / 自动决策
```

```md
<!-- CHANGELOG.md new section header -->
## 1.0.0 — 2026-06-08 · 交接点 Gap 校对器 · 双模式 · CLI 初版
```

```md
<!-- ROADMAP.md snippet -->
## 1.0.0 — 交接点 Gap 校对器
- Self-check / Receiver-check
- 交接点高成本 gap 识别与展示
- CLI runner + fixture regression
```

```md
<!-- CONTRIBUTING.md snippet -->
## 开发结构
- `skill/`：行为契约
- `engine/`：TypeScript 规则与输出管线
- `cli/`：命令行入口
- `eval/`：回归与样本评测
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/docs/doc-sync.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tests/verify-skill.md CHANGELOG.md ROADMAP.md CONTRIBUTING.md tests/docs/doc-sync.test.ts
git commit -m "docs: align verification and roadmap for 1.0.0"
```

### Task 11: Create soft-copyright-ready docs

**Files:**
- Create: `docs/user-manual.md`
- Create: `docs/architecture.md`
- Create: `docs/originality.md`
- Test: `tests/docs/soft-copyright-docs.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/docs/soft-copyright-docs.test.ts
import { describe, expect, it } from 'vitest'
import { readFileSync } from 'node:fs'

function read(path: string) {
  return readFileSync(path, 'utf8')
}

describe('soft copyright docs', () => {
  it('creates the required supporting documents', () => {
    expect(read('docs/user-manual.md')).toContain('软件说明书')
    expect(read('docs/architecture.md')).toContain('系统架构')
    expect(read('docs/originality.md')).toContain('原创性声明')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/docs/soft-copyright-docs.test.ts`
Expected: FAIL with missing docs

- [ ] **Step 3: Write minimal documentation**

```md
<!-- docs/user-manual.md -->
# 软件说明书

## 软件名称
TrustChain FindGap 智能请求缺口分析系统 1.0.0

## 适用场景
- 交接前 owner 自检
- 接手前 receiver 他检

## 核心流程
1. 找交接缺口
2. 校验依据
3. 展示并校对
```

```md
<!-- docs/architecture.md -->
# 系统架构

## 模块
- `parser`：输入解析
- `audience`：自检 / 他检视角
- `detector`：11 规则检测
- `severity`：严重度判定
- `evidence`：依据类型
- `fallback`：不确定性处理
- `advisor`：补齐方向
- `formatter`：结果展示
```

```md
<!-- docs/originality.md -->
# 原创性声明

## 原创部分
- 交接点 Gap 校对产品定义
- 11 条规则及其 handoff 场景优先级
- Self-check / Receiver-check 双模式
- 固定输出契约与依据类型分层

## 第三方依赖
- Node.js runtime
- TypeScript
- Vitest
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/docs/soft-copyright-docs.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add docs/user-manual.md docs/architecture.md docs/originality.md tests/docs/soft-copyright-docs.test.ts
git commit -m "docs: add soft copyright support docs"
```

### Task 12: Demote FindAction and FindMiss repo messaging

**Files:**
- Modify: `/Users/lihu/WorkSpace/ai/FindAction/README.md`
- Modify: `/Users/lihu/WorkSpace/ai/FindMiss/README.md`
- Test: `tests/docs/sibling-repo-messaging.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/docs/sibling-repo-messaging.test.ts
import { describe, expect, it } from 'vitest'
import { readFileSync } from 'node:fs'

const action = readFileSync('/Users/lihu/WorkSpace/ai/FindAction/README.md', 'utf8')
const miss = readFileSync('/Users/lihu/WorkSpace/ai/FindMiss/README.md', 'utf8')

describe('sibling repo messaging', () => {
  it('marks sibling repos as roadmap/experimental and points to FindGap', () => {
    expect(action).toContain('Experimental')
    expect(action).toContain('FindGap')
    expect(miss).toContain('Experimental')
    expect(miss).toContain('FindGap')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/docs/sibling-repo-messaging.test.ts`
Expected: FAIL because README banners do not exist

- [ ] **Step 3: Update sibling READMEs minimally**

```md
<!-- prepend to /Users/lihu/WorkSpace/ai/FindAction/README.md and /Users/lihu/WorkSpace/ai/FindMiss/README.md -->
> ⚠️ **Status: Experimental / Roadmap**
>
> This repository is a future TrustChain direction and is not the primary active product.
> If you want the current production-facing TrustChain tool, use **FindGap**:
> https://github.com/trustchain-ai/FindGap
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/docs/sibling-repo-messaging.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add /Users/lihu/WorkSpace/ai/FindAction/README.md /Users/lihu/WorkSpace/ai/FindMiss/README.md tests/docs/sibling-repo-messaging.test.ts
git commit -m "docs: demote sibling repo positioning"
```

---

## Self-Review

### Spec coverage
- **FindGap 1.0.0 as the only active product:** Covered by Tasks 1, 10, and 12.
- **Handoff-time checker positioning:** Covered by Tasks 1, 3, 7, and 10.
- **Self-check / Receiver-check dual modes:** Covered by Tasks 2, 3, 8, and 10.
- **High-cost professional gap identification and display:** Covered by Tasks 4, 5, 6, and 7.
- **Best practices as secondary guidance:** Covered by Tasks 6 and 7.
- **README full alignment:** Covered by Task 1 and Phase 1 wording.
- **Soft-copyright-friendly deliverables:** Covered by Task 11 and parts of Tasks 1 and 10.
- **FindAction / FindMiss repo demotion:** Covered by Task 12.

### Placeholder scan
- No `TODO`, `TBD`, or “implement later” placeholders remain.
- Every code-changing step includes concrete code.
- Every run step includes an exact command and expected result.

### Type consistency
- `FindGapMode` is consistently `self-check | receiver-check`.
- `EvidenceType` is consistently `public-evidence | contextual-inference | insufficient-information`.
- `GapFinding` fields are reused consistently across detector, evidence, fallback, advisor, formatter, and CLI tasks.
- CLI commands consistently use `check`, `eval`, and `report`.
